using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Variaveis : MonoBehaviour {

	// tipos básicos: inteiro = int, ponto flutuante = float, texto = string, booleano = bool 
	// tipo de acesso (opcional) + tipo da variável  (obrigtório) + nome da variável (obrigatório) + = valor (opcional)
	public int meuInteiro;
	public float meuFlutuandte;
	public string meuTexto;
	public bool meuBooleano;
	public int velocidadeDoMeuPersonagem;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
